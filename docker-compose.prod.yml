services:
  faq-assistant:
    build: .
    container_name: ai-faq-assistant
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    volumes:
      - ./knowledge:/app/knowledge:ro
      - ./data:/app/data
    restart: unless-stopped
