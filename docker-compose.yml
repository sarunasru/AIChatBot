services:
  faq-assistant:
    build: .
    container_name: ai-faq-assistant
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./knowledge:/app/knowledge:ro
    restart: unless-stopped
